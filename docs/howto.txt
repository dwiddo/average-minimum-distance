To create the .rst files, one per module, that build the html:

    sphinx-apidoc -o [output_dir] [package_dir] --separate

    $ ...\docs> sphinx-apidoc -o source .. --separate

To clean/build the html:

    $ ...\docs> make clean
    $ ...\docs> make html
